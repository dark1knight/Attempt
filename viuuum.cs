using UnityEngine;
using System.Collections;

public class viuuum : MonoBehaviour {
    public AudioSource fiuu;
	// Use this for initialization
	void Start () {
        fiuu = gameObject.GetComponent<AudioSource>();     
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
